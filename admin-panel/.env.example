# Backend API base URL (required for production, set in Railway)
# Example: https://your-backend.railway.app
VITE_API_URL=

# Optional: for local dev proxy when backend runs elsewhere
# VITE_DEV_PROXY_TARGET=http://localhost:3000
