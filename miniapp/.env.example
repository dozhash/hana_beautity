# Backend API URL for production or when backend is on a different host.
# Leave empty to use relative URLs (works with Vite dev proxy when backend runs on localhost:3000).
# When using ngrok for backend: run `ngrok http 3000` and set to that HTTPS URL.
# VITE_API_URL=https://your-backend.ngrok-free.dev
